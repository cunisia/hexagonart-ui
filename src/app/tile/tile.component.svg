@if(!!cartesianCoords) {
  <svg:use 
    class="tile" 
    xlink:href="#hexagon" 
    [ngStyle]="{fill: getColor()}"
    [attr.x]="cartesianCoords.x"
    [attr.y]="cartesianCoords.y"
    (click)="colorTile()">
  a:{{tile.a}}, r:{{tile.r}}, c:{{tile.c}}
  </svg:use>
}