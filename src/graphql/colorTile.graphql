mutation ColorTile($input: ColorTileInput!) {
  colorTile(input: $input) {
    message
    success
    colorEvent {
      id
      createdAt
      tileId
      userId
      r
      g
      b
    }
  }
}
